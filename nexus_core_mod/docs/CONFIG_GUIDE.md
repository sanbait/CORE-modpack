# Руководство по конфигурации Lux System

## 🎮 Настройка через GUI (НОВОЕ!)

**Теперь можно настраивать мод прямо из игры!**

### Как открыть настройки

1. Запустите Minecraft
2. В главном меню нажмите кнопку **"Mods"**
3. Найдите **"Nexus Core"** в списке модов
4. Нажмите кнопку **⚙️ Config** (или "Настройки")
5. Редактируйте параметры в удобном интерфейсе
6. Нажмите **"Done"** для сохранения

### Категории настроек

- **Nexus Core Settings** - настройки ядра (радиус, HP, улучшения)
- **Lighting Settings** - настройки освещения
- **Lux System** - настройки люкса (емкость, расход, дефолтные значения)

### Преимущества GUI

- ✅ Не нужно искать файл конфига
- ✅ Не нужно знать синтаксис TOML
- ✅ Подсказки для каждого параметра
- ✅ Валидация значений (мин/макс)
- ✅ Удобное редактирование списков
- ✅ Полная русская локализация

---

## 📁 Ручная настройка через файл (альтернатива)

## Расположение файла конфигурации

После первого запуска мода, конфигурационный файл будет создан автоматически:

```
.minecraft/config/nexuscore-common.toml
```

## Структура конфигурации

### 1. Настройки Nexus Core

#### Базовые параметры ядра

```toml
[Nexus Core Settings]
    # Базовый радиус для аггро и баффов на уровне 1
    baseRadius = 12.0
    # Дополнительный радиус за каждый уровень
    radiusPerLevel = 2.0
    # Здоровье за уровень
    hpPerLevel = 200.0
```

#### Стоимость улучшений

```toml
    # Формат: "modid:item_id|количество"
    upgradeCosts = [
        "luxsystem:lux_crystal|4",           # 1→2
        "luxsystem:lux_crystal|8",           # 2→3
        "luxsystem:lux_crystal|16",          # 3→4
        "luxsystem:concentrated_lux_crystal|2", # 4→5
        ...
    ]
```

### 2. Система освещения

```toml
[Lighting Settings]
    # Включить динамическое освещение для игроков
    enablePlayerLights = true
    
    # Предметы, излучающие свет
    playerLightItems = [
        "minecraft:torch",
        "minecraft:soul_torch",
        "minecraft:lantern",
        ...
    ]
```

### 3. Система Люкса (LUX SYSTEM) ⭐

Это основная секция для настройки всех параметров люкса!

#### 3.1 Емкость люкса для предметов

```toml
[Lux System]
    # Определяет максимальную емкость люкса для каждого предмета
    # Формат: "modid:item_id|емкость"
    itemLuxCapacities = [
        # Мечи
        "minecraft:wooden_sword|100",
        "minecraft:stone_sword|200",
        "minecraft:iron_sword|500",
        "minecraft:golden_sword|1000",
        "minecraft:diamond_sword|2000",
        "minecraft:netherite_sword|5000",
        
        # Кирки
        "minecraft:wooden_pickaxe|100",
        "minecraft:stone_pickaxe|200",
        "minecraft:iron_pickaxe|500",
        "minecraft:golden_pickaxe|1000",
        "minecraft:diamond_pickaxe|2000",
        "minecraft:netherite_pickaxe|5000"
    ]
```

**Пример настройки:**

- Деревянная кирка: 100 люкса
- Алмазная кирка: 2000 люкса
- Незеритовая кирка: 5000 люкса

#### 3.2 Расход люкса за использование ⚡

```toml
    # Определяет расход люкса за одно использование предмета
    # Формат: "modid:item_id|расход"
    itemLuxCosts = [
        # Мечи (расход за удар)
        "minecraft:wooden_sword|1",
        "minecraft:stone_sword|2",
        "minecraft:iron_sword|3",
        "minecraft:golden_sword|5",
        "minecraft:diamond_sword|5",
        "minecraft:netherite_sword|10",
        
        # Кирки (расход за блок)
        "minecraft:wooden_pickaxe|1",
        "minecraft:stone_pickaxe|2",
        "minecraft:iron_pickaxe|3",
        "minecraft:golden_pickaxe|5",
        "minecraft:diamond_pickaxe|5",
        "minecraft:netherite_pickaxe|10"
    ]
```

**Пример настройки:**

- Деревянная кирка: 1 люкс за блок
- Алмазная кирка: 5 люкса за блок
- Незеритовая кирка: 10 люкса за блок

#### 3.3 Дефолтные значения расхода

```toml
    # Расход люкса по умолчанию для ломания блоков
    # (если предмет не указан в itemLuxCosts)
    defaultBlockBreakCost = 5
    
    # Расход люкса по умолчанию для атаки
    # (если предмет не указан в itemLuxCosts)
    defaultAttackCost = 5
```

#### 3.4 Разрешить использование без люкса

```toml
    # Можно ли использовать инструменты/оружие без люкса?
    # true = работает без люкса (но не тратит его)
    # false = требует люкс для работы
    allowUseWithoutLux = true
```

**Важно:**

- Если `allowUseWithoutLux = false`:
  - Инструменты **не смогут ломать блоки** без люкса
  - Оружие будет наносить **50% урона** без люкса
  - Игрок получит сообщение о нехватке люкса

#### 3.5 Генерация и хранение люкса ядром

```toml
    # Люкс, генерируемый за тик на уровень ядра
    coreLuxGenerationPerLevel = 1
    
    # Максимальный люкс, хранимый на уровень ядра
    coreLuxCapacityPerLevel = 1000
```

**Пример:**

- Ядро уровня 5:
  - Генерирует: 5 люкса/тик
  - Хранит: 5000 люкса максимум

## Примеры настройки

### Пример 1: Хардкорный режим

```toml
# Высокий расход, низкая емкость
itemLuxCapacities = [
    "minecraft:diamond_pickaxe|500"  # Вместо 2000
]
itemLuxCosts = [
    "minecraft:diamond_pickaxe|20"   # Вместо 5
]
allowUseWithoutLux = false  # Требует люкс!
```

### Пример 2: Казуальный режим

```toml
# Низкий расход, высокая емкость
itemLuxCapacities = [
    "minecraft:diamond_pickaxe|10000"  # Вместо 2000
]
itemLuxCosts = [
    "minecraft:diamond_pickaxe|1"      # Вместо 5
]
allowUseWithoutLux = true  # Работает без люкса
```

### Пример 3: Добавление модовых предметов

```toml
itemLuxCapacities = [
    # Ванильные предметы...
    "minecraft:diamond_pickaxe|2000",
    
    # Предметы из других модов
    "mekanism:atomic_disassembler|50000",
    "thermal:flux_drill|10000",
    "create:mechanical_drill|3000"
]

itemLuxCosts = [
    # Ванильные предметы...
    "minecraft:diamond_pickaxe|5",
    
    # Предметы из других модов
    "mekanism:atomic_disassembler|100",
    "thermal:flux_drill|20",
    "create:mechanical_drill|8"
]
```

## Как найти ID предмета?

1. **В игре с F3+H:**
   - Нажмите F3+H
   - Наведите на предмет в инвентаре
   - ID будет показан в подсказке

2. **Формат ID:**

   ```
   modid:item_name
   ```

   Примеры:
   - `minecraft:diamond_pickaxe`
   - `luxsystem:lux_crystal`
   - `mekanism:atomic_disassembler`

## Перезагрузка конфигурации

После изменения конфигурации:

1. Сохраните файл `nexuscore-common.toml`
2. Выполните `/reload` в игре (или перезапустите мир)
3. Изменения вступят в силу

## Расчет длительности использования

**Формула:**

```
Использований = Емкость / Расход
```

**Примеры:**

- Алмазная кирка (2000 люкса, 5 за блок) = 400 блоков
- Незеритовый меч (5000 люкса, 10 за удар) = 500 ударов
- Деревянная кирка (100 люкса, 1 за блок) = 100 блоков

## Советы по балансу

1. **Емкость должна быть кратна расходу** для удобства
2. **Более мощные инструменты** = больше емкость + больше расход
3. **Золотые инструменты** могут иметь высокую емкость (быстрая зарядка)
4. **Незеритовые инструменты** должны быть самыми эффективными
5. **Учитывайте скорость зарядки** от ядра при балансе

## Поддержка

Если у вас возникли проблемы с конфигурацией:

1. Проверьте синтаксис TOML
2. Убедитесь, что ID предметов написаны правильно
3. Проверьте логи на ошибки
4. Удалите конфиг и дайте ему пересоздаться
