# 🎯 Быстрая настройка Lux - Шпаргалка

## 📍 Где находится конфиг?

```
.minecraft/config/nexuscore-common.toml
```

## ⚡ Быстрые настройки

### Изменить емкость инструмента

```toml
itemLuxCapacities = [
    "minecraft:diamond_pickaxe|2000"  # ← Измените число
]
```

### Изменить расход за использование

```toml
itemLuxCosts = [
    "minecraft:diamond_pickaxe|5"  # ← Измените число
]
```

### Дефолтные значения

```toml
defaultBlockBreakCost = 5  # ← Расход при ломании блоков
defaultAttackCost = 5      # ← Расход при атаке
```

### Требовать люкс для работы?

```toml
allowUseWithoutLux = true   # ← true = работает без люкса
                            #   false = требует люкс
```

## 📊 Примеры балансировки

### Вариант 1: Много емкости, мало расхода (легко)

```toml
"minecraft:diamond_pickaxe|10000"  # Емкость
"minecraft:diamond_pickaxe|1"      # Расход
# = 10000 блоков
```

### Вариант 2: Средняя емкость, средний расход (баланс)

```toml
"minecraft:diamond_pickaxe|2000"   # Емкость
"minecraft:diamond_pickaxe|5"      # Расход
# = 400 блоков
```

### Вариант 3: Мало емкости, много расхода (хардкор)

```toml
"minecraft:diamond_pickaxe|500"    # Емкость
"minecraft:diamond_pickaxe|10"     # Расход
# = 50 блоков
```

## 🔢 Калькулятор использований

```
Количество использований = Емкость ÷ Расход
```

**Примеры:**

- 2000 ÷ 5 = **400 блоков**
- 5000 ÷ 10 = **500 ударов**
- 100 ÷ 1 = **100 блоков**

## 🛠️ Шаблон для добавления нового предмета

```toml
# 1. Добавьте емкость
itemLuxCapacities = [
    # ... существующие предметы ...
    "modid:item_name|емкость"
]

# 2. Добавьте расход
itemLuxCosts = [
    # ... существующие предметы ...
    "modid:item_name|расход"
]
```

## 🎮 Как узнать ID предмета?

1. В игре нажмите **F3 + H**
2. Наведите на предмет в инвентаре
3. ID будет показан в подсказке

**Формат:** `modid:item_name`

## 🔄 Применить изменения

После редактирования конфига:

1. Сохраните файл
2. В игре: `/reload`
3. Готово!

## 💡 Советы

- **Емкость** должна быть кратна **расходу** (удобнее считать)
- **Незерит** > **Алмаз** > **Железо** > **Камень** > **Дерево**
- Для **хардкора**: `allowUseWithoutLux = false`
- Для **казуала**: высокая емкость + низкий расход

## ⚠️ Важно

- Используйте правильный синтаксис TOML
- Не забывайте запятые между элементами
- ID предметов пишутся **строчными** буквами
- Формат: `"modid:item|число"`
