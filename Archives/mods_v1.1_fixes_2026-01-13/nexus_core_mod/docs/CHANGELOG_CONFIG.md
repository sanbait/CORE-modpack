# Changelog - Система конфигурации Lux

## Дата: 2026-01-11

### ✨ Новые возможности

#### 1. Полная система конфигурации для Lux System

Добавлена возможность настройки **всех** параметров люкса через конфигурационный файл `config/nexuscore-common.toml`.

#### 2. Настройка емкости люкса

**Файл:** `NexusCoreConfig.java`

- Параметр: `itemLuxCapacities`
- Формат: `"modid:item_id|емкость"`
- Пример: `"minecraft:diamond_pickaxe|2000"`

**Что можно настроить:**

- Максимальная емкость люкса для каждого инструмента/оружия
- Поддержка любых модовых предметов

#### 3. Настройка расхода люкса ⚡ (НОВОЕ!)

**Файл:** `NexusCoreConfig.java`

- Параметр: `itemLuxCosts`
- Формат: `"modid:item_id|расход"`
- Пример: `"minecraft:diamond_pickaxe|5"`

**Что можно настроить:**

- Расход люкса за одно использование (удар/блок)
- Индивидуальный расход для каждого предмета

#### 4. Дефолтные значения расхода (НОВОЕ!)

**Параметры:**

- `defaultBlockBreakCost` - расход при ломании блоков (по умолчанию: 5)
- `defaultAttackCost` - расход при атаке (по умолчанию: 5)

**Применение:**

- Используются для предметов, не указанных в `itemLuxCosts`

#### 5. Режим работы без люкса (НОВОЕ!)

**Параметр:** `allowUseWithoutLux`

- `true` - инструменты работают без люкса (не тратят его)
- `false` - требуется люкс для использования

**Поведение при `false`:**

- Инструменты **не ломают блоки** без люкса
- Оружие наносит **50% урона** без люкса
- Показываются сообщения о нехватке люкса

### 🔧 Изменения в коде

#### `NexusCoreConfig.java`

```java
// Добавлены новые параметры:
+ ITEM_LUX_COSTS
+ DEFAULT_BLOCK_BREAK_COST
+ DEFAULT_ATTACK_COST
+ ALLOW_USE_WITHOUT_LUX
```

#### `NexusCore.java`

```java
// Добавлен метод получения расхода из конфига
+ getCostFromConfig(ItemStack stack)

// Обновлена логика ломания блоков
~ onBlockBreak() - теперь использует конфиг вместо хардкода

// Обновлена логика атаки
~ onLivingHurt() - теперь использует конфиг вместо хардкода
```

#### Локализация

**en_us.json:**

```json
+ "message.nexuscore.no_lux": "§cNot enough Lux to use this tool!"
+ "message.nexuscore.low_lux": "§eLow Lux! Reduced damage."
```

**ru_ru.json:**

```json
+ "message.nexuscore.no_lux": "§cНедостаточно Люкса для использования!"
+ "message.nexuscore.low_lux": "§eМало Люкса! Урон снижен."
```

### 📚 Документация

Создан файл `docs/CONFIG_GUIDE.md` с подробным руководством:

- Описание всех параметров
- Примеры настройки
- Советы по балансу
- Расчет длительности использования

### 🎯 Примеры использования

#### Пример 1: Настройка деревянной кирки

```toml
itemLuxCapacities = ["minecraft:wooden_pickaxe|100"]
itemLuxCosts = ["minecraft:wooden_pickaxe|1"]
```

**Результат:** 100 блоков до разрядки

#### Пример 2: Настройка алмазной кирки

```toml
itemLuxCapacities = ["minecraft:diamond_pickaxe|2000"]
itemLuxCosts = ["minecraft:diamond_pickaxe|5"]
```

**Результат:** 400 блоков до разрядки

#### Пример 3: Хардкорный режим

```toml
allowUseWithoutLux = false
defaultBlockBreakCost = 10
defaultAttackCost = 10
```

**Результат:** Инструменты требуют люкс, высокий расход

### 🔄 Обратная совместимость

- Все старые конфиги будут работать
- Новые параметры имеют дефолтные значения
- Поведение по умолчанию не изменилось

### 📝 TODO (будущие улучшения)

- [ ] Добавить настройку скорости зарядки от ядра
- [ ] Добавить настройку расхода для брони
- [ ] Добавить настройку бонусов при полном заряде
- [ ] GUI для редактирования конфига в игре

### 🐛 Исправленные проблемы

- ❌ Хардкод значения расхода люкса (было: 5)
- ✅ Теперь полностью настраиваемо через конфиг

### 🎮 Как использовать

1. Запустите игру с модом
2. Найдите файл `config/nexuscore-common.toml`
3. Отредактируйте нужные параметры
4. Выполните `/reload` или перезапустите мир
5. Наслаждайтесь настроенным балансом!

---

**Автор:** Antigravity AI  
**Дата:** 2026-01-11  
**Версия мода:** 1.0.0+
