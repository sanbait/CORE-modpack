# Unified Development Pipeline (Agent Protocol)

Этот документ описывает единый алгоритм действий (Pipeline), который Агент (Lead Architect) обязан выполнять при любой задаче.

## 🔁 The Cycle (Цикл Разработки)

### 1. 🔍 Phase 1: SCAN & ANALYZE (Разведка)

**Перед написанием любой строки кода:**

1. **Check Mods:** Просканировать папку `mods/`. Есть ли мод, который уже делает это?
    - *Если да:* Предложить конфиг вместо скрипта.
2. **Check Docs:** Свериться с `docs/GDD/` и `docs/TechGD/`.
    - *Вопрос:* Соответствует ли задача ЛОРу и Балансу?
    - *Вопрос:* Есть ли уже занятые ID предметов в `docs/tech/`?
3. **Check Scripts:** Проверить `kubejs/server_scripts/`.
    - *Цель:* Не дублировать переменные и не ломать существующие Event Listeners.

### 2. 🧠 Phase 2: PLAN & DESIGN (Проектирование)

**Создание плана (Implementation Plan):**

1. **Atmosphere Check:** Проходит ли решение проверку на "Безысходность"?
    - *Плохо:* "Блок дает полет".
    - *Хорошо:* "Механизм подбрасывает игрока ценой взрыва пара".
2. **Technical Check:**
    - Использовать `Level Event` или `Server Tick`? (Избегать тяжелых тиков).
    - Нужен ли NBT или хватит Block State?
3. **Approval:** Согласовать план с Пользователем.

### 3. 🔨 Phase 3: EXECUTE (Кодинг)

**Стандарты KubeJS:**

1. **Modular:** Скрипт класть в соответствующую папку (`core`, `entropy`, `alchemy`).
2. **Logging:** `console.log('[CORE_DEBUG] ...')` на ключевых действиях.
3. **No Magic:** Рецепты через Create (Mixing, Pressing), а не просто Crafting Table.

### 4. 📝 Phase 4: DOCUMENT & COMMIT (Фиксация)

**Принцип Истины:**

1. **Update Docs:** Если изменили урон/HP/рецепт — СНАЧАЛА обновить `docs/`.
2. **Commit:** Использовать Conventional Commits (`feat:`, `fix:`, `balance:`).

---

## 🚨 Emergency Protocols (Критические Правила)

* **Never Delete:** Никогда не удалять файлы без явной команды или бэкапа.
- **Stop on Error:** Если `kubejs/startup.txt` выдал ошибку — СТОП. Анализ лога. Не пытаться "угадать" фикс.
- **User Block:** Если задача нарушает ЛОР (например, "добавь магазин оружия"), Агент обязан возразить и предложить альтернативу в стиле Dark Fantasy.
