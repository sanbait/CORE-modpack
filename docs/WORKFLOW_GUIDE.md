# Протокол Разработки Entropy Core (v2.1)

Этот документ — единственный источник истины о том, как мы работаем.

---

## 🛑 1. ЗОЛОТОЕ ПРАВИЛО "SAVE & PUSH"

**Работа не считается законченной, пока она не в облаке.**

Каждая сессия (или крупная задача) должна заканчиваться двоеточием:

1. **COMMIT (Локально):** Сохраняет изменения на диске.
2. **PUSH (В Облако):** Отправляет изменения на GitHub.

**Агент ОБЯЗАН:**

* В конце задачи создать скрипт для коммита.
* Сразу после успеха коммита напомнить: *"А теперь сделай PUSH!"* или запустить `push_to_github.bat`.

---

## 🔄 2. Полный Пайплайн (Workflow)

### ФАЗА 1: Подготовка

1. **Sync:** Читаем `docs/TechGD/TECH_DESIGN_CORE.md`.
2. **Scan:** Смотрим `mods/` и `kubejs/`.
3. **Plan:** Пишем план в `implementation_plan.md`.

### ФАЗА 2: Безопасность

1. **BACKUP:** Перед кодом — архив в `Archives/`.
2. **Git Status:** Проверяем, чисто ли в репозитории.

### ФАЗА 3: Код & Тест

1. Пишем код (модульно).
2. Запускаем сборку/игру.
3. Смотрим логи.

### ФАЗА 4: Финализация (The "Close-Out")

1. **DOCS:** Обновляем README мода, TechGD, Changelog.
2. **GIT COMMIT:** Скрипт `git_commit.bat`.
3. **GIT PUSH:** Скрипт `push_to_github.bat`. **<-- ВАЖНО!**
4. **SUMMARY:** Пишем саммери для следующей сессии.

---

## 🛠 Полезные Скрипты

* `save_progress.bat` — Быстрый коммит (WIP).
* `push_to_github.bat` — Отправка в облако.
* `nexus_core_mod/build.bat` — (Если есть) сборка мода.

---

*Последнее обновление: 11.01.2026*
