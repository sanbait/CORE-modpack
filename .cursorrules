# Nexus Core Project Rules

## CRITICAL INSTRUCTION

**ALWAYS READ `docs/TechGD/TECH_DESIGN_CORE.md` FIRST.**
This file contains the absolute truth about the project state, architecture, and recent changes. Ignore any chat history that conflicts with it.

## Project Context

- **Modloader**: Forge 1.20.1
- **Architecture**:
  - `NexusCoreEntity` is a **GeckoLib Entity** (Not a BlockEntity).
  - Lux System Items use **Capabilities** on Vanilla Items (No custom textures needed).
- **Localization**: Strictly separated. `nexuscore` strings in `nexuscore/lang`, `luxsystem` strings in `luxsystem/lang`.
